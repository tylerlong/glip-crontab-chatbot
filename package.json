{
  "license": "MIT",
  "scripts": {
    "dev": "node -r @babel/register -r dotenv/config src/express.js",
    "ngrok": "ngrok http -region eu 3000",
    "build": "babel src --watch --out-dir dist --source-maps",
    "deploy": "sls deploy",
    "test": "jest"
  },
  "dependencies": {
    "cron-parser": "^2.7.1",
    "ringcentral-chatbot": "^0.1.7"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.6",
    "@babel/preset-env": "^7.1.6",
    "@babel/register": "^7.0.0",
    "babel-core": "7.0.0-bridge.0",
    "babel-jest": "^23.6.0",
    "dotenv": "^6.1.0",
    "jest": "^23.6.0",
    "ngrok": "^3.1.0",
    "serverless": "^1.33.1",
    "sqlite3": "^4.0.4",
    "standard": "^12.0.1",
    "yarn-upgrade-all": "^0.5.0"
  },
  "standard": {
    "ignore": [
      "/dist/"
    ]
  },
  "yarn-upgrade-all": {
    "ignore": [
      "babel-core"
    ]
  }
}
